spring:
  application:
    name: newlens-backend

server:
  port: 8080

app:
  news:
    guardian:
      base-url: https://content.guardianapis.com
      api-key: ${GUARDIAN_API_KEY:}
      timeout-seconds: 5
